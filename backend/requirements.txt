    # FastAPI and ASGI server
fastapi==0.135.1
uvicorn[standard]==0.41.0
websockets==13.1
python-multipart==0.0.22

# Pydantic for data validation
pydantic==2.11.10
pydantic-settings==2.13.1

# Supabase client
supabase==2.28.0
postgrest==2.28.0

# JWT handling
pyjwt[crypto]==2.10.1
cryptography==46.0.5

# AI/ML dependencies (simplified for now - can add vector store later)
llama-index==0.10.0
groq==0.4.2
tavily-python==0.7.12
sentence-transformers==2.3.1  # FREE local embeddings

# Database
psycopg2-binary==2.9.9
pgvector==0.2.4

# HTTP client
# NOTE: supabase/gotrue currently expects httpx with 'proxy' client arg support.
httpx==0.27.2
aiohttp==3.10.5
redis==5.0.8

# Utilities
python-dotenv==1.0.0
python-jose[cryptography]==3.3.0

# Logging and monitoring
loguru==0.7.2

# Testing
pytest==8.3.5
